import PortfolioList from '@/components/portfolio-list'
import { mediaPipePortfolio } from '@/config/docs'

export default function MediaPipePortfolioList() {
  return <PortfolioList portfolio={mediaPipePortfolio} />
}
